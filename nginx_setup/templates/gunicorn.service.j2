[Unit]
Description=gunicorn daemon
After=network.target

[Service]
User=gunicorn
Group=nginx
WorkingDirectory={{ project_dir }}
ExecStart={{ venv_dir }}/bin/gunicorn --access-logfile - --workers 3 --bind unix:{{ sock }} {{ project_name }}.wsgi:application

[Install]
WantedBy=multi-user.target
